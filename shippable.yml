branches:
  only:
    - develop
    - test

build:
  ci:
    - sudo apt-get update
    - sudo apt-get install cmake
    - ./.ci/run_test

integrations:
  notifications:
    - integrationName: email
      type: email
      on_success: change
      on_failure: change
      sendConsoleLogs: true
